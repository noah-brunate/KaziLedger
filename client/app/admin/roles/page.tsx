import { WorkspacePage } from '@/components/workspace-page';

export default function AdminRolesPage() {
  return <WorkspacePage workspace="admin" section="roles" title="Roles & access" description="Review account roles and keep access permissions aligned with each team member's work." />;
}
